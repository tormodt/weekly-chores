{
  "name": "weekly-chores",
  "version": "1.0.0",
  "description": "Weekly chores app - Web server deployment only",
  "main": "index.html",
  "scripts": {
    "start": "npx tsc && npx http-server . -p 3000 -o",
    "build": "npx tsc",
    "dev": "npx tsc --watch",
    "deploy": "npx tsc && git add . && git commit -m 'Deploy to GitHub Pages' && git push"
  },
  "dependencies": {
    "vue": "^3.5.21"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "http-server": "^14.1.1"
  }
}
